# diy-photobook — pinned dependencies
# Install-gate: every one of these must import on the target interpreter before
# any pipeline code runs. If a C-extension fails to build on a very new Python
# (e.g. 3.14), fall the whole environment back to a 3.12 venv (see docs/SETUP.md)
# — do not run on a half-broken env.

# --- Image decode / processing ---
pillow-heif==1.4.0          # HEIC/HEIF decode (iPhone default format)
rawpy==0.27.0               # RAW decode (only needed if you shoot RAW)
imagehash==4.3.2            # perceptual hashing for dedup (pHash)
piexif==1.1.3               # EXIF read/parse helper (never used to WRITE originals)

# --- Already expected present in the base env (pinned loosely for safety) ---
Pillow>=10.0
numpy>=1.26
pandas>=2.0
opencv-python-headless>=4.9,<5  # variance-of-Laplacian blur; HEADLESS to avoid GUI/Qt.
                                # <5: OpenCV 5.x drops cv2.CascadeClassifier, which
                                # photobook/faces.py needs for face-safe crops.

# --- Browser render (contact sheets + proof PDF) ---
playwright>=1.44

# --- CLI framework ---
click>=8.1
PyYAML>=6.0

# NOTE: exiftool is a standalone binary (exiftool.exe), NOT a pip package.
#       Install it separately and confirm `exiftool -ver` runs on PATH.
#       It is used READ-ONLY on your originals; EXIF is only ever WRITTEN onto
#       exported copies.
